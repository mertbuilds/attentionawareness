// Prism ships each grammar as a side-effect module that registers itself on the
// shared `Prism` object. `@types/prismjs` types the core but not these subpaths.
declare module 'prismjs/components/prism-css';
